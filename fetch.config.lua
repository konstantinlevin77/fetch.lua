_M = {}

_M.titles = {
    
    -- Displays the main title consists of username and hostname.
    mainTitle = true,
    -- Displays the name of the operating system.
    OSTitle = true,
    -- Displays the kernel version
    kernelVersion = true,
    -- Displays the uptime.
    uptime = true,
    -- Displays the current shell.
    shell = true,
    -- Displays the desktop environment.
    desktopEnvironment = true,
    -- Displays the window manager.
    windowManager = true,
    -- Displays the cpu.
    cpu = true,
    -- Displays the GPU.
    gpu = true,
    -- Displays the currently used memory.
    memory = true
    }


return _M
